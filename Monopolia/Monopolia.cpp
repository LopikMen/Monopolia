#include"App.h"
using namespace sf;
int main()
{
   /* App app();
    app.run();
    */
    //while (window.isOpen())
    //{
    //    sf::Event event;
    //    while (window.pollEvent(event))
    //    {
    //        if (event.type == sf::Event::Closed)
    //            window.close();
    //    }
    //   
    //    Image map_image;//объект изображения для карты
    //    map_image.loadFromFile("map.jpg");//загружаем файл для карты
    //    Texture map;//текстура карты
    //    map.loadFromImage(map_image);//заряжаем текстуру картинкой
    //    Sprite s_map;//создаём спрайт для карты
    //    s_map.setTexture(map);//заливаем текстуру спрайтом
    //    s_map.setPosition(1, 1);

    //    Image pl_image;//объект изображения для карты
    //    pl_image.loadFromFile("player1.png");//загружаем файл для карты
    //    Texture pl;//текстура карты
    //    pl.loadFromImage(pl_image);//заряжаем текстуру картинкой
    //    Sprite pla;//создаём спрайт для карты
    //    pla.setTexture(pl);//заливаем текстуру спрайтом
    //    pla.setPosition(1, 1);
    //    pla.setTextureRect(IntRect(0, SIZE_HERO_H, SIZE_HERO_W, SIZE_HERO_H));
    //    if (Keyboard::isKeyPressed(Keyboard::Left)) { pla.move(-0.1, 0); pla.setTextureRect(IntRect(0, SIZE_HERO_Y, SIZE_HERO_X, SIZE_HERO_Y)); } //координата Y, на которой герой изображен идущим влево равна 96
    //    if (Keyboard::isKeyPressed(Keyboard::Right)) { pla.move(0.1, 0); pla.setTextureRect(IntRect(0, 192, 96, 96)); } //координата Y, на которой герой изображен идущем вправо равна 96+96=192
    //    if (Keyboard::isKeyPressed(Keyboard::Up)) { pla.move(0, -0.1); pla.setTextureRect(IntRect(0, 288, 96, 96)); } //координата Y на которой герой изображен идущим вверх равна 288
    //    if (Keyboard::isKeyPressed(Keyboard::Down)) { pla.move(0, 0.1); pla.setTextureRect(IntRect(0, 0, 96, 96)); } //координата 0, это верхняя часть изображения с героем, от нее и отталкиваемся ровными квадратиками по 96. 


    //    window.clear();
    //    window.draw(s_map);
    //    window.draw(pla);
    //    window.display();
    //}

    
}